<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="funcentric.com.gsupdates">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        >
        <activity android:name="funcentric.com.gsupdates.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="funcentric.com.gsupdates.Data.DatabaseProvider"
            android:authorities="funcentric.com.gsupdates" />

        <activity android:name="funcentric.com.gsupdates.DataActivity" />
        <activity android:name="funcentric.com.gsupdates.DataSilver" />
        <activity android:name="funcentric.com.gsupdates.ChartsActivity" />
        <activity android:name="funcentric.com.gsupdates.SearchActivity" />
        <activity android:name="funcentric.com.gsupdates.DrawActivity"></activity>
    </application>

</manifest>